[build]
builder = "NIXPACKS"
# Proxy only needs ws — skip Vite/TypeScript frontend build
buildCommand = "npm install --omit=dev"

[deploy]
startCommand = "node proxy.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
